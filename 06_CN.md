# Computer Networks — Topic Map

> **The topic that cost the last offer.** The Eternal (Zomato) interview was
> failed on CN — TCP vs UDP could not be answered. That was a *revision* gap,
> not a comprehension gap, which is why CN is the only subject scheduled twice.
>
> **Block A — the floor.** Four items, done first, before anything else in the
> plan. If every other block collapses, the last failure still does not repeat.
> **Block G — the depth.** Full Tier 2 + Tier 3, scheduled last so it is
> freshest on the day.
>
> Legend and fill-in rule: [00_PLAN.md](00_PLAN.md).

---

# §A — THE FLOOR (Block A, non-negotiable, do first)

Four items. Nothing else starts until all four are `[x]` — explainable out loud,
cold, no notes.

- [ ] **[HOT]** OSI 7 layers and TCP/IP 4 layers — name them and what lives at each
- [ ] **[HOT] [ASKED]** **TCP vs UDP** — connection-oriented vs connectionless, reliability, ordering, flow control, congestion control, header size, use cases. *This is the exact question that was failed. Know it cold.*
- [ ] **[HOT]** TCP three-way handshake (SYN, SYN-ACK, ACK) and four-way teardown (FIN, ACK, FIN, ACK)
- [ ] **[HOT]** "What happens when you type a URL into a browser" — DNS → TCP handshake → TLS → HTTP request → response → render

---

# §G — THE DEPTH (Block G, full sweep)

## Tier 1 — Foundations, breadth first

- [ ] OSI 7 layers and TCP/IP 4 layers *(floor — see §A)*
- [ ] Encapsulation and headers as data moves down the stack
- [ ] **[HOT] [ASKED]** TCP vs UDP *(floor — see §A)*
- [ ] Client-server vs peer-to-peer
- [ ] Bandwidth vs latency vs throughput
- [ ] LAN, WAN, MAN

## Tier 2 — Core

### TCP internals

- [ ] **[HOT]** Three-way handshake and four-way teardown *(floor — see §A)*
- [ ] Sequence and acknowledgement numbers
- [ ] **[HOT]** Flow control (sliding window) vs congestion control (slow start, congestion avoidance, AIMD, fast retransmit)
- [ ] TIME_WAIT and why it exists

### The web path

- [ ] **[HOT]** URL → browser, three levels deep on **any** step *(floor — see §A)*
- [ ] DNS: recursive vs iterative resolution; record types (A, AAAA, CNAME, MX, NS); caching and TTL
- [ ] HTTP methods, status code classes, headers, cookies
- [ ] **[HOT]** HTTP/1.1 vs HTTP/2 vs HTTP/3 — pipelining, multiplexing, head-of-line blocking, QUIC over UDP
- [ ] **[HOT]** HTTPS and the TLS handshake; symmetric vs asymmetric encryption; certificates and chain of trust

### Addressing

- [ ] IP addressing; IPv4 vs IPv6; subnetting; CIDR
- [ ] Public vs private addresses; NAT
- [ ] ARP, DHCP, ICMP
- [ ] Ports, sockets, the 5-tuple connection identifier

## Tier 3 — Applied

- [ ] Router vs switch vs hub vs bridge
- [ ] Routing: static vs dynamic; distance vector vs link state; BGP at concept level
- [ ] **[HOT]** REST semantics: statelessness, idempotency, safe methods, which HTTP verbs are idempotent
- [ ] WebSockets vs long polling vs server-sent events
- [ ] Load balancers: L4 vs L7, algorithms, health checks
- [ ] Reverse proxy vs forward proxy
- [ ] CDNs
- [ ] Common attacks: DDoS, man-in-the-middle, DNS spoofing

---

## Resources

- *Computer Networking: A Top-Down Approach* (Kurose & Ross) — the standard.
- **Gate Smashers / Neso Academy** (YouTube) — for the breadth sweep.
- **High Performance Browser Networking** (free online, Ilya Grigorik) — excellent on TCP, TLS, HTTP/2.

## Block G drill list — every [HOT] in this file

Every item below gets asked cold and answered out loud. The four §A floor items
get drilled **twice** — once at the start of Block G, once on the last day.

- [ ] OSI and TCP/IP layers
- [ ] **[ASKED]** TCP vs UDP
- [ ] Three-way handshake and four-way teardown
- [ ] URL → browser, then a follow-up three levels deep on a step chosen at random
- [ ] Flow control vs congestion control
- [ ] TIME_WAIT
- [ ] HTTP/1.1 vs HTTP/2 vs HTTP/3
- [ ] TLS handshake; symmetric vs asymmetric; chain of trust
- [ ] REST: which verbs are idempotent, and why
